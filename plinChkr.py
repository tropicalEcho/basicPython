while True: import os, sys, re; cmd = input("~\\plinChkr$ ").strip().upper(); sys.exit("GOODBYE!") if cmd in ["EXIT", "QUIT"] else os.system("cls" if os.name == "nt" else "clear") if cmd in ["CLEAR", "CLS"] else print("\nCOMMANDS:\n  HELP            - Prints this.\n  EXIT | QUIT     - Kills plinChkr.\n  CLEAR | CLS     - Clears screen.\n  <WORD>          - Check if the given word is a palindrome.\n") if cmd in ["HELP"] else print("IT'S A PALINDROME" if (word := re.sub(r'[^A-Za-z0-9]', '', cmd)) == word[::-1] else "NOT A PALINDROME")